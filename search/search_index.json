{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"vCode","text":"<p>vCode is an ACP-first local coding agent built on top of <code>pydantic-ai</code>.</p> <p>The current codebase is intentionally narrow: it focuses on a solid skeleton for local coding sessions before adding more advanced agent patterns. The implemented surface today is centered around:</p> <ul> <li>ACP server support</li> <li>session-backed chat state</li> <li><code>Ask</code>, <code>Plan</code>, and <code>Agent</code> modes</li> <li>model selection and persistence</li> <li>session-scoped write approvals</li> <li>local workspace tools for list, read, and write</li> <li><code>.vcode/.vcodeignore</code> filtering for read and list operations</li> </ul>"},{"location":"#what-vcode-can-do-today","title":"What vCode can do today","text":"<ul> <li>Run as an ACP agent server over stdio</li> <li>Create, resume, fork, and list sessions</li> <li>Persist session history under <code>.vcode/sessions/&lt;session-id&gt;/</code></li> <li>Read and write local workspace files through tools</li> <li>Ask for write approvals with file-aware diff previews</li> <li>Store the selected default model in <code>.vcode/preferences.json</code></li> <li>Let each mode inherit the default model or override it per mode</li> <li>Compose the runtime from native <code>pydantic-ai</code> capabilities</li> <li>Load YAML-first MCP and hooks config from <code>.vcode/</code></li> <li>Attach configured MCP servers through native <code>MCP</code> capabilities</li> <li>Execute configured hook commands through <code>pydantic-ai Hooks</code></li> </ul>"},{"location":"#what-is-not-finished-yet","title":"What is not finished yet","text":"<p>These items are planned, but not part of the current working skeleton:</p> <ul> <li>terminal command execution as a real tool</li> <li>workspace text search</li> <li>built-in web search and scraping</li> <li>Python quality loop (<code>pytest</code>, <code>ruff</code>, <code>mypy</code>/<code>pyright</code>) as first-class tools</li> <li>subagents, CodeMode, RLM, and multi-agent orchestration</li> <li>richer hook actions and approval policy for <code>hooks.yml</code> / <code>hooks.json</code></li> </ul>"},{"location":"#documentation-map","title":"Documentation map","text":"<ul> <li><code>Getting Started</code>: how to install and run the ACP server</li> <li><code>Current Capabilities</code>: a feature-by-feature inventory</li> <li><code>ACP Integration</code>: current ACP methods, updates, and client behavior</li> <li><code>Configuration</code>: <code>.vcode</code> files and precedence rules</li> <li><code>Modes and Sessions</code>: mode semantics and session persistence</li> <li><code>Approvals</code>: current approval model and commands</li> <li><code>Workspace Tools</code>: read/list/write behavior, diffs, and <code>.vcodeignore</code></li> </ul>"},{"location":"acp/","title":"ACP Integration","text":"<p>vCode is designed to be ACP-first from the beginning.</p>"},{"location":"acp/#runtime-shape","title":"Runtime shape","text":"<p>The ACP entrypoint is a thin adapter around the local runtime:</p> <ul> <li><code>src/vcode/acp/server.py</code></li> <li><code>src/vcode/acp/agent.py</code></li> </ul> <p>The adapter:</p> <ul> <li>creates sessions</li> <li>forwards prompt turns to the runtime</li> <li>maps runtime state into ACP updates</li> <li>handles permission requests for write approvals</li> </ul>"},{"location":"acp/#session-bootstrap","title":"Session bootstrap","text":"<p>On session creation or load, vCode returns ACP session state that includes:</p> <ul> <li>modes</li> <li>models</li> <li>config options</li> </ul> <p>This lets ACP clients render:</p> <ul> <li>mode pickers</li> <li>model pickers</li> <li>session state</li> </ul>"},{"location":"acp/#config-options","title":"Config options","text":"<p>The current ACP config option surface includes:</p> <ul> <li><code>mode</code></li> <li><code>model</code></li> </ul> <p>The selected values are synchronized back into local state.</p>"},{"location":"acp/#tool-call-updates","title":"Tool call updates","text":"<p>Tool activity is emitted through ACP tool updates.</p> <p>Current mapping:</p> <ul> <li><code>list_files</code> -&gt; <code>search</code></li> <li><code>read_file</code> -&gt; <code>read</code></li> <li><code>write_file</code> -&gt; <code>edit</code></li> </ul> <p>Write operations send structured diff content so ACP clients that understand diffs can render them cleanly.</p> <p>Read and list operations send structured text content previews.</p>"},{"location":"acp/#permission-requests","title":"Permission requests","text":"<p>Write approvals are implemented through <code>session/request_permission</code>.</p> <p>The current permission request includes:</p> <ul> <li>a title</li> <li>structured <code>raw_input</code></li> <li>file locations</li> <li>diff content for writes</li> </ul> <p>This is especially important for ACP clients such as <code>toad</code>, which can render file diffs in approval dialogs.</p>"},{"location":"acp/#hook-and-mcp-visibility","title":"Hook and MCP visibility","text":"<p>ACP clients now also receive projections for:</p> <ul> <li>hook command executions as generic <code>execute</code> tool updates</li> <li>non-filesystem tools, including MCP-backed tools, as generic execute-style tool updates</li> </ul> <p>This means MCP and hook activity is no longer invisible in ACP sessions, even when the tool family does not map to a specialized ACP kind such as <code>read</code> or <code>edit</code>.</p>"},{"location":"acp/#known-current-limits","title":"Known current limits","text":"<p>The ACP surface is already useful, but still incomplete.</p> <p>Missing or partial areas:</p> <ul> <li>terminal capability integration</li> <li>filesystem client-backed fallback behavior</li> <li>richer session metadata</li> <li>search and browser tools</li> <li>richer MCP approval and policy routing</li> </ul>"},{"location":"approvals/","title":"Approvals","text":"<p>The current approval system is intentionally narrow and explicit.</p>"},{"location":"approvals/#what-is-implemented","title":"What is implemented","text":"<p>Today, approvals are enforced for:</p> <ul> <li><code>write_file</code></li> </ul> <p>That is the only tool family with a real approval gate in the current runtime.</p>"},{"location":"approvals/#scope","title":"Scope","text":"<p>Approval decisions are session-scoped.</p> <p>This means:</p> <ul> <li>a saved approval applies only to the current session</li> <li>opening a new session starts from a clean approval state</li> <li>old approvals can be imported manually from another session</li> </ul>"},{"location":"approvals/#storage","title":"Storage","text":"<p>Rules are saved in:</p> <pre><code>.vcode/sessions/&lt;session-id&gt;/approvals.json\n</code></pre>"},{"location":"approvals/#manual-commands","title":"Manual commands","text":"<p>Supported commands:</p> <ul> <li><code>/approvals</code></li> <li><code>/approve write &lt;path&gt;</code></li> <li><code>/deny write &lt;path&gt;</code></li> <li><code>/update-preferences &lt;session-id&gt;</code></li> </ul>"},{"location":"approvals/#acp-approval-flow","title":"ACP approval flow","text":"<p>When a write is not already approved, the ACP adapter sends a permission request with:</p> <ul> <li>title</li> <li>tool kind</li> <li>file location</li> <li>raw input</li> <li>diff content</li> </ul> <p>This lets ACP clients render a meaningful approval dialog instead of a blind allow/deny prompt.</p>"},{"location":"approvals/#write-diff-behavior","title":"Write diff behavior","text":"<p>For write approvals, vCode compares:</p> <ul> <li>the current file contents, if the file exists</li> <li>the proposed new contents</li> </ul> <p>That diff is sent through ACP as structured diff content.</p>"},{"location":"approvals/#yolo-behavior","title":"YOLO behavior","text":"<p>There is a <code>yolo_default</code> preference in <code>preferences.json</code>.</p> <p>Current meaning:</p> <ul> <li>if enabled, write approval checks are bypassed</li> </ul> <p>This is intentionally off by default.</p>"},{"location":"approvals/#current-limitation","title":"Current limitation","text":"<p>Approvals are not yet wired into:</p> <ul> <li>terminal commands</li> <li>network access</li> <li>browser automation</li> <li>MCP tools</li> </ul>"},{"location":"configuration/","title":"Configuration","text":"<p>vCode currently reads configuration from <code>.vcode/</code> and optionally from <code>~/.vcode/</code>.</p>"},{"location":"configuration/#precedence","title":"Precedence","text":"<p>Configuration resolution is file-by-file:</p> <ul> <li>use <code>project/.vcode/&lt;file&gt;</code> if it exists</li> <li>otherwise fall back to <code>~/.vcode/&lt;file&gt;</code></li> </ul> <p>This means local project config wins over global config.</p>"},{"location":"configuration/#current-files","title":"Current files","text":""},{"location":"configuration/#preferencesjson","title":"<code>preferences.json</code>","text":"<p>This is the only config file actively used by the current runtime.</p> <p>Example:</p> <pre><code>{\n  \"default_mode\": \"agent\",\n  \"default_model\": \"groq:openai/gpt-oss-120b\",\n  \"mode_models\": {\n    \"ask\": \"openai:gpt-5-mini\",\n    \"plan\": \"anthropic:claude-sonnet-4-5\"\n  },\n  \"yolo_default\": false,\n  \"history_compaction\": \"auto\",\n  \"external_docs_lookup\": false,\n  \"skill_discovery\": \"session_start\",\n  \"web\": {\n    \"search\": {\n      \"provider\": \"searx\",\n      \"searx_base_url\": \"\"\n    },\n    \"scrape\": {\n      \"provider\": \"builtin\"\n    },\n    \"browser\": {\n      \"provider\": \"browser-use\"\n    }\n  }\n}\n</code></pre> <p>Important current fields:</p> <ul> <li><code>default_mode</code></li> <li><code>default_model</code></li> <li><code>mode_models</code></li> <li><code>yolo_default</code></li> </ul>"},{"location":"configuration/#vcodeignore","title":"<code>.vcodeignore</code>","text":"<p>This file is checked by workspace read/list tools.</p> <p>Current behavior:</p> <ul> <li>ignored paths do not appear in <code>list_files</code></li> <li>ignored files cannot be read by <code>read_file</code></li> </ul> <p>Current implementation supports simple gitignore-like glob patterns, but not full gitignore semantics. In particular, negation patterns beginning with <code>!</code> are not active yet.</p>"},{"location":"configuration/#agentsjson","title":"<code>agents.json</code>","text":"<p>This file is loadable and schema-checked, but not yet actively consumed by the runtime.</p> <p>Current typed shape:</p> <pre><code>{\n  \"python\": {\n    \"model\": \"openai:gpt-5-mini\"\n  }\n}\n</code></pre> <p>Only the <code>model</code> field is currently validated.</p>"},{"location":"configuration/#mcpyml","title":"<code>mcp.yml</code>","text":"<p>MCP config is YAML-first.</p> <p>Resolution order:</p> <ul> <li><code>.vcode/mcp.yml</code></li> <li><code>.vcode/mcp.yaml</code></li> <li><code>.vcode/mcp.json</code></li> <li><code>~/.vcode/mcp.yml</code></li> <li><code>~/.vcode/mcp.yaml</code></li> <li><code>~/.vcode/mcp.json</code></li> </ul> <p>JSON remains supported, but <code>mcp.yml</code> is the native format.</p> <p>Environment variable interpolation is supported for fields such as:</p> <ul> <li><code>command</code></li> <li><code>args</code></li> <li><code>url</code></li> <li><code>env</code></li> </ul> <p>The current runtime reads this file and builds native <code>pydantic-ai</code> MCP capabilities from it.</p> <p>Example:</p> <pre><code>servers:\n  - name: demo-local\n    transport: stdio\n    command: python3.11\n    args:\n      - scripts/demo_mcp_server.py\n    prefix: demo\n\n  - name: searx-local\n    transport: http\n    url: ${SEARX_MCP_URL}\n    prefix: searx\n    enabled: false\n</code></pre> <p>Tracked local demo server:</p> <ul> <li><code>scripts/demo_mcp_server.py</code></li> </ul>"},{"location":"configuration/#hooksyml","title":"<code>hooks.yml</code>","text":"<p>Hooks config is also YAML-first.</p> <p>Resolution order:</p> <ul> <li><code>.vcode/hooks.yml</code></li> <li><code>.vcode/hooks.yaml</code></li> <li><code>.vcode/hooks.json</code></li> <li><code>~/.vcode/hooks.yml</code></li> <li><code>~/.vcode/hooks.yaml</code></li> <li><code>~/.vcode/hooks.json</code></li> </ul> <p>Current scope:</p> <ul> <li>the file is loadable and typed</li> <li>configured commands are bridged into native <code>pydantic-ai</code> <code>Hooks</code></li> <li>event ids match <code>pydantic-ai</code> hook lifecycle names</li> </ul> <p>Example:</p> <pre><code>events:\n  before_tool_execute:\n    - name: audit-write\n      command: python3.11\n      args:\n        - scripts/mock_hook_audit.py\n      tools:\n        - write_file\n\n  after_model_request:\n    - name: snapshot-model-response\n      command: python3.11\n      args:\n        - scripts/mock_hook_snapshot.py\n</code></pre> <p>Current behavior:</p> <ul> <li>commands run with the workspace root as <code>cwd</code></li> <li>configured <code>env</code> entries are merged into the process environment</li> <li>optional <code>name</code> gives a stable display label for <code>/hooks</code> and ACP hook projections</li> <li>optional <code>tools</code> filters a hook command to matching tool names or glob patterns</li> <li><code>VCODE_HOOK_EVENT</code></li> <li><code>VCODE_HOOK_WORKSPACE_ROOT</code></li> <li><code>VCODE_HOOK_SESSION_ID</code></li> <li><code>VCODE_HOOK_MODE_ID</code></li> <li><code>VCODE_HOOK_PAYLOAD_JSON</code>   are injected for each hook command</li> </ul> <p>Inspection commands:</p> <ul> <li><code>/hooks</code> shows the resolved hook configuration for the current workspace</li> <li><code>/mcp</code> shows the resolved MCP server configuration for the current workspace</li> </ul> <p>Tracked local demo scripts:</p> <ul> <li><code>scripts/mock_hook_audit.py</code></li> <li><code>scripts/mock_hook_snapshot.py</code></li> </ul>"},{"location":"configuration/#session-storage","title":"Session storage","text":"<p>Session state lives under:</p> <pre><code>.vcode/sessions/&lt;session-id&gt;/\n</code></pre> <p>Per-session files currently include:</p> <ul> <li><code>session.json</code></li> <li><code>history.jsonl</code></li> <li><code>messages.json</code></li> <li><code>approvals.json</code></li> </ul>"},{"location":"current-capabilities/","title":"Current Capabilities","text":"<p>This page lists what the current codebase actually implements.</p>"},{"location":"current-capabilities/#acp-runtime","title":"ACP runtime","text":"<p>Implemented ACP-facing behavior:</p> <ul> <li><code>initialize</code></li> <li><code>authenticate</code></li> <li><code>new_session</code></li> <li><code>fork_session</code></li> <li><code>load_session</code></li> <li><code>resume_session</code></li> <li><code>list_sessions</code></li> <li><code>prompt</code></li> <li><code>set_session_mode</code></li> <li><code>set_session_model</code></li> <li><code>set_config_option</code></li> </ul> <p>The ACP adapter also emits:</p> <ul> <li>available commands updates</li> <li>current mode updates</li> <li>config option updates</li> <li>tool call start/progress updates</li> <li>permission requests for write approvals</li> </ul>"},{"location":"current-capabilities/#session-model","title":"Session model","text":"<p>Sessions are first-class and stored on disk.</p> <p>Each session currently persists:</p> <ul> <li>session id</li> <li>workspace path</li> <li>active mode</li> <li>title</li> <li>human-readable chat history</li> <li>serialized model message history</li> <li>imported approval source sessions</li> </ul>"},{"location":"current-capabilities/#modes","title":"Modes","text":"<p>Three modes exist:</p> <ul> <li><code>Ask</code>: read-only</li> <li><code>Plan</code>: read workspace, write only under <code>.vcode/plans/</code></li> <li><code>Agent</code>: full edit mode, still gated by approvals</li> </ul> <p>Mode state is represented both in the runtime and through ACP mode/config surfaces.</p>"},{"location":"current-capabilities/#model-management","title":"Model management","text":"<p>vCode currently supports:</p> <ul> <li>listing known model ids from <code>pydantic-ai</code></li> <li>selecting a default model</li> <li>selecting per-mode model overrides</li> <li>exposing model state to ACP clients</li> </ul> <p>Current storage:</p> <ul> <li><code>.vcode/preferences.json</code></li> <li><code>.vcode/mcp.yml</code> with JSON fallback</li> <li><code>.vcode/hooks.yml</code> with JSON fallback</li> </ul>"},{"location":"current-capabilities/#capability-stack","title":"Capability stack","text":"<p>The runtime is assembled from native <code>pydantic-ai</code> capabilities:</p> <ul> <li><code>Toolset(build_filesystem_toolset(...))</code></li> <li><code>PrepareTools(...)</code> for mode-aware filesystem visibility</li> <li><code>Hooks(...)</code> built from <code>.vcode/hooks.yml</code></li> <li><code>MCP(...)</code> or stdio MCP toolsets from <code>.vcode/mcp.yml</code></li> </ul>"},{"location":"current-capabilities/#workspace-tools","title":"Workspace tools","text":"<p>Implemented local filesystem tools:</p> <ul> <li><code>list_files</code></li> <li><code>read_file</code></li> <li><code>write_file</code></li> </ul> <p>Manual demo assets also exist in the repository:</p> <ul> <li><code>scripts/demo_mcp_server.py</code></li> <li><code>scripts/mock_hook_audit.py</code></li> <li><code>scripts/mock_hook_snapshot.py</code></li> </ul> <p>Current behavior:</p> <ul> <li>reads are plain text reads</li> <li>writes are mode-gated</li> <li><code>Ask</code> mode hides <code>write_file</code> from the model through <code>PrepareTools</code></li> <li>writes require deferred approval unless already allowed for the session</li> <li><code>.vcode/.vcodeignore</code> hides matching files from read/list</li> </ul>"},{"location":"current-capabilities/#approval-system","title":"Approval system","text":"<p>Implemented today:</p> <ul> <li>session-scoped write approvals</li> <li>manual allow/deny commands</li> <li>importing approval preferences from another session</li> <li>ACP permission prompts with diff previews for writes</li> <li>native deferred approvals through <code>pydantic-ai</code></li> <li>hook commands executed through <code>pydantic-ai Hooks</code></li> </ul> <p>Not implemented yet:</p> <ul> <li>terminal approvals</li> <li>network approvals</li> <li>browser approvals</li> <li>multi-tool approval policies</li> <li>approval gating for hook command execution</li> </ul>"},{"location":"current-capabilities/#slash-commands","title":"Slash commands","text":"<p>Currently handled in the runtime:</p> <ul> <li><code>/models</code></li> <li><code>/model</code></li> <li><code>/model &lt;model-id&gt;</code></li> <li><code>/model &lt;ask|plan|agent&gt; &lt;model-id&gt;</code></li> <li><code>/approvals</code></li> <li><code>/hooks</code></li> <li><code>/mcp</code></li> <li><code>/approve &lt;tool&gt; &lt;target&gt;</code></li> <li><code>/deny &lt;tool&gt; &lt;target&gt;</code></li> <li><code>/update-preferences &lt;session-id&gt;</code></li> </ul>"},{"location":"current-capabilities/#test-coverage","title":"Test coverage","text":"<p>The repository currently has tests for:</p> <ul> <li>session behavior</li> <li>ACP adapter behavior</li> <li>capability composition</li> <li>config loading</li> <li>approval persistence</li> <li>model command flows</li> <li>workspace read/write flows</li> <li><code>.vcodeignore</code> read/list filtering</li> </ul>"},{"location":"getting-started/","title":"Getting Started","text":"<p>This page documents the current working way to run vCode.</p>"},{"location":"getting-started/#requirements","title":"Requirements","text":"<ul> <li>Python 3.11+</li> <li>the project installed in an environment with:</li> <li><code>agent-client-protocol</code></li> <li><code>pydantic-ai</code></li> </ul>"},{"location":"getting-started/#install","title":"Install","text":"<p>Preferred local install:</p> <pre><code>uv pip install -e .\n</code></pre> <p><code>pip</code> fallback:</p> <pre><code>pip install -e .\n</code></pre> <p>If you want docs support too:</p> <pre><code>uv pip install -e .[docs]\n</code></pre> <p><code>pip</code> fallback:</p> <pre><code>pip install -e .[docs]\n</code></pre>"},{"location":"getting-started/#run-the-acp-server","title":"Run the ACP server","text":"<p>vCode currently exposes a single runtime mode: ACP over stdio.</p> <pre><code>vcode\n</code></pre> <p>or explicitly:</p> <pre><code>vcode acp\n</code></pre> <p>Internally this calls <code>run_agent(VCodeAcpAgent())</code>.</p>"},{"location":"getting-started/#first-run-local-state","title":"First-run local state","text":"<p>vCode writes project-local state under <code>.vcode/</code>.</p> <p>Important files and directories:</p> <ul> <li><code>.vcode/preferences.json</code></li> <li><code>.vcode/.vcodeignore</code></li> <li><code>.vcode/sessions/&lt;session-id&gt;/session.json</code></li> <li><code>.vcode/sessions/&lt;session-id&gt;/history.jsonl</code></li> <li><code>.vcode/sessions/&lt;session-id&gt;/messages.json</code></li> <li><code>.vcode/sessions/&lt;session-id&gt;/approvals.json</code></li> </ul>"},{"location":"getting-started/#select-a-model","title":"Select a model","text":"<p>The agent needs a configured model before a normal prompt turn can run.</p> <p>Supported models come from <code>pydantic_ai.models.KnownModelName</code>.</p> <p>Commands:</p> <ul> <li><code>/models</code></li> <li><code>/models groq</code></li> <li><code>/model</code></li> <li><code>/model groq:openai/gpt-oss-120b</code></li> <li><code>/model ask openai:gpt-5-mini</code></li> <li><code>/model plan anthropic:claude-sonnet-4-5</code></li> </ul> <p>When you set a model, vCode writes it to <code>.vcode/preferences.json</code>.</p>"},{"location":"getting-started/#current-acp-clients","title":"Current ACP clients","text":"<p>The current implementation is built for ACP clients such as:</p> <ul> <li>terminal/TUI ACP clients</li> <li>editor ACP clients such as Zed</li> </ul> <p>Behavior can differ slightly by client, especially around:</p> <ul> <li>command autocomplete</li> <li>model picker UI</li> <li>permission dialog rendering</li> <li>diff rendering</li> </ul>"},{"location":"limitations/","title":"Current Limitations","text":"<p>This page is intentionally blunt. It lists what the current implementation does not do yet.</p>"},{"location":"limitations/#tooling-gaps","title":"Tooling gaps","text":"<p>Not implemented as first-class tools yet:</p> <ul> <li>terminal execution</li> <li>text search</li> <li>regex search</li> <li>web search</li> <li>scraping</li> <li>browser automation</li> </ul>"},{"location":"limitations/#quality-loop-gaps","title":"Quality loop gaps","text":"<p>Not implemented yet:</p> <ul> <li><code>pytest</code> as a tool</li> <li><code>ruff</code> as a tool</li> <li><code>mypy</code> or <code>pyright</code> as a tool</li> <li>automatic repair loops around lint or test failures</li> </ul>"},{"location":"limitations/#config-gaps","title":"Config gaps","text":"<p>These surfaces still exist, but are not yet full runtime bootstrap surfaces:</p> <ul> <li><code>agents.json</code></li> </ul> <p><code>mcp.yml</code> / <code>mcp.json</code> is active and builds native MCP capabilities, but still has current limits:</p> <ul> <li>approval policy is not yet wired into MCP tool calls</li> <li>collision strategy still relies on explicit prefixes when needed</li> <li>there is no richer agent-level MCP orchestration yet</li> </ul> <p><code>hooks.yml</code> / <code>hooks.json</code> is active for command hooks, but still limited:</p> <ul> <li>no approval layer for hook commands yet</li> <li>no richer hook action types beyond command execution</li> </ul>"},{"location":"limitations/#approval-gaps","title":"Approval gaps","text":"<p>Approval enforcement currently applies only to file writes.</p> <p>Missing:</p> <ul> <li>command approvals</li> <li>network approvals</li> <li>browser approvals</li> <li>tool family policy matching</li> </ul>"},{"location":"limitations/#ignore-semantics","title":"Ignore semantics","text":"<p><code>.vcode/.vcodeignore</code> is useful now, but still limited.</p> <p>Not implemented:</p> <ul> <li>negation patterns with <code>!</code></li> <li>full gitignore parity</li> <li>ignore-aware search tooling</li> </ul>"},{"location":"limitations/#advanced-agent-features","title":"Advanced agent features","text":"<p>Explicitly not part of the current bare skeleton:</p> <ul> <li>subagents</li> <li>deep agent orchestration</li> <li>CodeMode</li> <li>recursive language model loops</li> <li>durable multi-agent workflows</li> </ul>"},{"location":"sessions-and-modes/","title":"Modes and Sessions","text":"<p>vCode is session-centric.</p>"},{"location":"sessions-and-modes/#session-lifecycle","title":"Session lifecycle","text":"<p>The ACP adapter currently supports:</p> <ul> <li>create new session</li> <li>load existing session</li> <li>resume session</li> <li>fork session</li> <li>list sessions</li> </ul> <p>Each session gets a UUID-based id and its own directory under <code>.vcode/sessions/</code>.</p>"},{"location":"sessions-and-modes/#what-a-session-stores","title":"What a session stores","text":"<p>vCode keeps two forms of history:</p> <ul> <li>human-readable chat history in <code>history.jsonl</code></li> <li>serialized model message state in <code>messages.json</code></li> </ul> <p>This split matters because ACP/UI replay and model continuation are related but not identical.</p>"},{"location":"sessions-and-modes/#session-titles","title":"Session titles","text":"<p>The first user prompt becomes the session title unless the title is still the default placeholder.</p>"},{"location":"sessions-and-modes/#mode-semantics","title":"Mode semantics","text":""},{"location":"sessions-and-modes/#ask","title":"Ask","text":"<ul> <li>can read workspace files</li> <li>can read plan files</li> <li>cannot write workspace files</li> <li>does not currently expose terminal execution</li> </ul>"},{"location":"sessions-and-modes/#plan","title":"Plan","text":"<ul> <li>can read the workspace</li> <li>can write only under <code>.vcode/plans/</code></li> <li>intended for planning artifacts, not code changes</li> </ul>"},{"location":"sessions-and-modes/#agent","title":"Agent","text":"<ul> <li>can write the workspace</li> <li>still goes through approvals</li> <li>is the default mode today</li> </ul>"},{"location":"sessions-and-modes/#mode-selection","title":"Mode selection","text":"<p>Mode can be changed through ACP and is persisted in the session record.</p> <p>Default mode comes from:</p> <ul> <li><code>.vcode/preferences.json</code></li> <li>fallback: <code>agent</code></li> </ul>"},{"location":"sessions-and-modes/#cross-session-preference-import","title":"Cross-session preference import","text":"<p>Approval rules are session-scoped by default.</p> <p>If you want to reuse decisions from an older session, use:</p> <pre><code>/update-preferences &lt;session-id&gt;\n</code></pre> <p>Current behavior:</p> <ul> <li>merge import</li> <li>if a rule already exists in the current session, the current session wins</li> </ul>"},{"location":"workspace-tools/","title":"Workspace Tools","text":"<p>The current local runtime exposes a minimal workspace toolset.</p>"},{"location":"workspace-tools/#implemented-tools","title":"Implemented tools","text":"<ul> <li><code>list_files(path=\".\")</code></li> <li><code>read_file(path)</code></li> <li><code>write_file(path, content)</code></li> </ul> <p>These are normal agent tools used by the <code>pydantic-ai</code> loop.</p>"},{"location":"workspace-tools/#list_files","title":"<code>list_files</code>","text":"<p>Behavior:</p> <ul> <li>lists files recursively</li> <li>skips <code>__pycache__</code></li> <li>hides paths ignored by <code>.vcode/.vcodeignore</code></li> <li>returns a simple newline-delimited listing</li> </ul> <p>In ACP, <code>list_files</code> is projected as a <code>search</code>-kind tool call with text content preview.</p>"},{"location":"workspace-tools/#read_file","title":"<code>read_file</code>","text":"<p>Behavior:</p> <ul> <li>reads a text file from the workspace</li> <li>truncates long files</li> <li>rejects ignored paths from <code>.vcode/.vcodeignore</code></li> </ul> <p>In ACP, <code>read_file</code> is projected as a <code>read</code>-kind tool call with file location and text preview.</p>"},{"location":"workspace-tools/#write_file","title":"<code>write_file</code>","text":"<p>Behavior:</p> <ul> <li>resolves the path inside the workspace root</li> <li>checks mode rules</li> <li>checks the approval policy</li> <li>writes the file if allowed</li> </ul> <p>Mode interaction:</p> <ul> <li><code>Ask</code>: blocked</li> <li><code>Plan</code>: only <code>.vcode/plans/**</code></li> <li><code>Agent</code>: allowed, but still approval-gated</li> </ul> <p>In ACP, <code>write_file</code> is projected as an <code>edit</code>-kind tool call with structured diff content.</p>"},{"location":"workspace-tools/#vcodevcodeignore","title":"<code>.vcode/.vcodeignore</code>","text":"<p>Current ignore behavior is scoped to workspace reading and listing.</p> <p>Examples:</p> <pre><code>secret.txt\nprivate/\n*.secret\n</code></pre> <p>Current guarantees:</p> <ul> <li>ignored files do not appear in <code>list_files</code></li> <li>ignored files are rejected by <code>read_file</code></li> </ul> <p>Current non-goal:</p> <ul> <li>full gitignore compatibility</li> </ul>"},{"location":"workspace-tools/#current-limitation","title":"Current limitation","text":"<p>There is no dedicated text search tool yet.</p> <p>Planned future additions include:</p> <ul> <li>regex/text search</li> <li>terminal execution</li> <li>web search and scrape tools</li> </ul>"},{"location":"about/","title":"About vCode","text":"<p>vCode is being built as a local-first coding agent with ACP as its primary public integration surface.</p>"},{"location":"about/#product-direction","title":"Product direction","text":"<p>The near-term goal is not maximum feature count. The goal is a reliable core that most coding agents share:</p> <ul> <li>ACP-compatible agent runtime</li> <li>local workspace read/write loop</li> <li>session persistence</li> <li>explicit modes</li> <li>approval-gated actions</li> <li>model selection</li> </ul> <p>Once that base is stable, more advanced features can be layered on top:</p> <ul> <li>terminal execution</li> <li>search and retrieval</li> <li>web tooling</li> <li>Python quality loops</li> <li>subagents</li> <li>CodeMode</li> <li>recursive language model patterns</li> </ul>"},{"location":"about/#current-engineering-bias","title":"Current engineering bias","text":"<p>vCode is currently optimized for becoming a strong Python coding agent first. That means the architecture is being shaped around:</p> <ul> <li>strict session state</li> <li>explicit approval boundaries</li> <li>type-safe Python code</li> <li>ACP compatibility from the beginning</li> </ul>"},{"location":"about/#license","title":"License","text":"<p>The project metadata currently declares an MIT license in <code>pyproject.toml</code>.</p>"}]}